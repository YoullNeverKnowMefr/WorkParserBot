# --- Telegram API credentials (https://my.telegram.org -> API development tools)
API_ID=34272266
API_HASH=794bd05afb28d3bd2b4c3a761517f4d2

# Bot token from @BotFather (renders the inline keyboard in the moderation group)
BOT_TOKEN=8983388450:AAFeFvgbixyjcal-h8NUnJlBIK8CmoOIc1A

# Session file names for the two clients
SESSION=observer
BOT_SESSION=modbot

# Channels to monitor (job sources). @usernames or -100... ids, comma separated.
SOURCE_CHANNELS=@livinginarmenia, @uptown_yerevan, @armeniatime, @ii_community, @IT_Portal, @PushEnter, @rnd_afisha, @kuda161, @events161ru, @rgb_rostov_events, @rostov_deti, @child_events_rostov, @trk_megamag

# Closed moderation group id (-100...). Add BOTH the observer account and the bot to it.
MODERATION_GROUP=-1002116502478

# Target channel where approved vacancies are published (observer must be able to post).
TARGET_CHANNEL=-1004384420833

# Optional: only these user ids may press the buttons (comma separated). Empty = anyone in group.
MODERATOR_IDS=

# Who may use the bot's control panel (start/stop, login, logout). Comma separated user ids.
# STRONGLY recommended — login controls the observer account. Empty = anyone can control it.
# Don't know your id? Start the bot and send it any message; it replies with "Ваш ID: ...".
ADMIN_IDS=8569476923 375543179 7362573437

# Start parsing automatically on launch if the observer is already logged in (true/false).
PARSING_ON_START=true

# Optional: override the default keyword list (comma separated, case-insensitive).
# KEYWORDS=вакансия,ищем,требуется,нужен,hiring,looking for

# Delete the forwarded message + keyboard after publishing (true/false)
DELETE_AFTER_PUBLISH=true

ADMIN_IDS=8569476923 375543179 7362573437
# Optional: path to the categories file
# CATEGORIES_FILE=categories.json
